# 2026-06-13 작업 노트 — OV5640 PCLK / Reset 디버깅

## 오늘 해결한 것

### 1. Reset 문제 해결
- 카메라가 USB 재연결 없이는 PCLK를 출력하지 않던 문제
- 원인: 커넥터 핀 매핑 오류 — G20이 NC가 아니라 **RST** 핀으로 직결되어 있었음
- 수정: XDC에서 `ov5640_reset` M18 → **G20** 으로 변경
- 커넥터 최종 핀 매핑:
  ```
  G20 → RST (camera)
  G19 → PWDN (camera)
  ```

### 2. VIO soft reset 로직 수정 (`top_ov5640_hdmi.v`)
- 기존 DEBUG 모드: `assign ov5640_reset = ~dbg_soft_reset;` (VIO 직결, 타이밍 없음)
- 수정 후: pwrseq_cnt 기반으로 PWDN/RESET 시퀀스 정상 실행
  ```verilog
  assign ov5640_pwdn  = (pwrseq_cnt < 20'd25000);
  assign ov5640_reset = (pwrseq_cnt >= 20'd50000);
  ```
- VIO 토글 시: pwrseq_cnt → 0 → PWDN HIGH(~1ms) → RESET HIGH(~2ms) → I2C 재시작(~16ms)

### 3. LUT 조건부 컴파일 구조 도입 (`lut_ov5640_rgb565_1280_720.v`)
- `ifdef / `else / `endif 로 20 MHz / 24 MHz XCLK 분기
- 파일 맨 위에 정의:
  ```verilog
  `define USE_20MHz_XTAL  // EBAZ4205 기본 20MHz 크리스탈
  ```
- 변경된 레지스터:
  | 인덱스 | 레지스터 | 24 MHz (원본) | 20 MHz (`ifdef 활성) |
  |--------|----------|--------------|----------------------|
  | 209    | 0x3036   | 0x69 (N=105) | 0x7E (N=126)         |
  | 252    | 0x3036   | 0x69 (N=105) | 0x7E (N=126)         |

- 이유: VCO를 동일하게 840 MHz 유지
  - 원본: 24/3 × 105 = 840 MHz
  - 변경: 20/3 × 126 = 840 MHz

## 오늘 확인한 사실

### PCLK 현황
- 현재 PCLK = **20 MHz** (목표 84 MHz 미달)
- N 변경 → PCLK 비례 변화 확인 (PLL 동작 중)
- VCO = 840 MHz, 출력 분주기 = 42 (÷42 → 20 MHz)
- 원본 설계 기준 출력 분주기 = 10 (÷10 → 84 MHz) 와 불일치

### 0x3103 레지스터 (SCCB SYSTEM CTRL1)
- **bit[1]**: 시스템 클럭 소스 선택
  - 0 = pad clock (XCLK 핀 직통)
  - 1 = PLL 출력
- bit[4]는 debug 비트 → 변경 금지
- 0x3103은 PLL 기준 클럭(XCLK vs RC) 선택 레지스터가 **아님**

### 0x3824 = 0x04 (auto PCLK mode 의심)
- bit[4] = 0 → auto PCLK 분주기 계산 모드
- 1280×720 타이밍 레지스터가 20 MHz 기준으로 맞지 않아 분주기가 ÷42로 잘못 계산될 가능성

## 다음 세션 실험 목록

1. **0x3824 manual mode**: 0x04 → 0x14 (bit[4]=1 활성, auto 끄기)  
   → PCLK가 84 MHz로 오르는지 확인
2. PLL lock 상태 추가 확인 방법 탐색
3. HREF 주기 재확인 (현재 ~752 pixel/line → VGA급, 1280×720 미적용 의심)

## 기타 사항

- XDC 파일 인라인 주석 (`#` 같은 줄) → Vivado 파싱 오류 발생, **반드시 별도 줄**에 작성
- `ifdef 방식: 하드웨어 변형(주파수, 보드 차이)에 적합, 원본값이 `else`에 남아 직관적
- git commit: `7853f8e >>>PCLK 4MHz with reset by G20`
